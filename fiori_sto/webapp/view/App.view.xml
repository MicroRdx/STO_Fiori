<mvc:View controllerName="fioristo.controller.App"
    xmlns:html="http://www.w3.org/1999/xhtml"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <App id="app">
    <content>
            <Text text="This is a home page"/>
        			<ComboBox
				items="{
					path: '/combo1List'
				
				}">
				<core:Item key="{key}" text="{text}" />
			</ComboBox>
                    			<ComboBox
				items="{
					path: '/combo2List'
			
				}">
				<core:Item key="{key}" text="{text}" />
			</ComboBox>
    </content>
    </App>
</mvc:View>
